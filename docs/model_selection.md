# Model selection

Generated by `python -m scripts.benchmark_models` on 2026-09-21.

Only models available on the Groq free tier for this account were considered.
The Llama family is not available on this account, so the selection was made
from what the key could actually reach.

## Latency

5 runs per model, one realistic agent turn each, wall clock including network.

| Model | Mean | Min | Max |
|---|---|---|---|
| `qwen/qwen3.8-27b` | 139 ms | 119 ms | 202 ms |
| `openai/gpt-oss-20b` | 480 ms | 420 ms | 527 ms |
| `openai/gpt-oss-120b` | 425 ms | 308 ms | 514 ms |
| `groq/compound-mini` | 813 ms | 550 ms | 1057 ms |

## Capability checks

| Model | JSON mode | Grounded refusal |
|---|---|---|
| `qwen/qwen3.8-27b` | pass | pass |
| `openai/gpt-oss-20b` | pass | pass |
| `openai/gpt-oss-120b` | pass | pass |
| `groq/compound-mini` | pass | pass |

## Multilingual output

**`qwen/qwen3.8-27b`**

- Taglish: Depende po sa edad, coverage amount, at investment fund na pipiliin niyo, so iba-iba ang premium. Maaari po ba kayong magbigay ng details para ma-quote ko na agad ang exact amount?
- Bahasa: Tenang, Mbak, biasanya telat 3 hari kena denda sekitar 0,5% dari sisa pokok cicilan atau minimal Rp50.000. Silakan cek detailnya di aplikasi atau tanyakan ke CS kami untuk angka pastinya.

**`openai/gpt-oss-20b`**

- Taglish: Magkano po ang premium ng VUL plan?  Depende po sa coverage at edad, pero karaniwang mula ₱2,000 hanggang ₱5,000 bawat buwan.  Paki‑contact po ako para sa eksaktong quote!
- Bahasa: Maaf, Mbak. Untuk cicilan telat 3 hari biasanya denda 1% per hari, jadi kira‑kira Rp 30.000 (1% × Rp 3.000.000). Pastikan segera bayar sebelum jatuh tempo ya!

**`openai/gpt-oss-120b`**

- Taglish: Depende po sa edad, health condition, at coverage amount ninyo, pero karaniwang nagsisimula ito sa around ₱2,000–₱5,000 kada buwan para sa basic na VUL plan. Para sa eksaktong quote, puwede po natin i
- Bahasa: Keterlambatan 3 hari biasanya dikenakan denda sekitar 0,5 % – 1 % dari total cicilan, tergantung tenor dan kebijakan jatuh tempo bank. Kalau mau pastiin, bisa cek di aplikasi atau hubungi CS kami ya.

**`groq/compound-mini`**

- Taglish: Depende po yan sa inyong edad, health condition, at coverage na gusto ninyo; kung bibigyan niyo po ako ng ilang detalye (edad, coverage amount, at term), makakalkula ko po agad ang premium.
- Bahasa: Untuk telat 3 hari, biasanya akan dikenakan denda 0,5 % × 3 hari dari jumlah cicilan (minimal Rp10.000), jadi cek kembali besaran cicilan Anda di kontrak untuk pastinya. Selamat melunasi!

## Decision

On latency the ordering was: `qwen/qwen3.8-27b` at 139 ms, `openai/gpt-oss-120b` at 425 ms, `openai/gpt-oss-20b` at 480 ms, `groq/compound-mini` at 813 ms.

`qwen/qwen3.8-27b` was 3.1 times faster than the next candidate, `openai/gpt-oss-120b`. That margin is the deciding factor, because a live nudge competes with the pace of a real conversation.

**Chosen: `qwen/qwen3.8-27b` throughout, with `openai/gpt-oss-20b` as fallback.**

Three findings shaped this, and two of them contradicted the initial plan:

1. The plan called for a two-tier split, a large model for conversation and a
   small fast one for live signal extraction. The measurement removed the need.
   The fastest model was also competitive on every quality axis, so a split
   would have added complexity while making the conversation path worse.

2. The gpt-oss models initially looked far weaker at Taglish and Bahasa, returning
   empty or truncated replies. The cause was not language ability: they spend the
   token budget on internal reasoning before emitting an answer. Setting
   `reasoning_effort` to `low` fixed it, and their multilingual output is in fact
   sound. Reporting the first result would have been a measurement error, not a
   finding, which is why the harness sets that parameter explicitly.

3. `groq/compound-mini` is excluded despite workable quality. It performs its own
   web searches, so its answers are not constrained to the knowledge base. For a
   system whose central requirement is that every answer is grounded and citable,
   a model that can silently reach the open web is disqualifying regardless of how
   well it scores.

`openai/gpt-oss-20b` serves as the fallback because it comes from a different
model family, so an issue specific to one provider's weights does not take the
whole system down mid-demo.

## Reproducing

```bash
python -m scripts.benchmark_models
```

Numbers move between runs because this is a shared free tier measured over the
public internet. The ordering has been stable across runs; the absolute values
are not.